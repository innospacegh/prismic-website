import * as React from 'react'
import styled from 'styled-components'

const SpacesSectionContainer = styled.section``

const SpacesSection = () => <SpacesSectionContainer />

export default SpacesSection
